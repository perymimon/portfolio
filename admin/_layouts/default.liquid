<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="{{ "/assets/grow-avatar-2.png" | relative_url }}">
    <link type="application/atom+xml" rel="alternate" href="{{ site.baseurl }}/feed.xml" title="{{ site.title }}" />

    <title>{{ page.title | default: site.title }} | {{ site.title }}</title>
    {% seo %}
    <link rel="alternate" type="application/atom+xml" title="{{ site.title }}" href="{{ site.url }}{{ site.baseurl }}/feed.xml">
    {% for style in page.styles %}
        <link rel="stylesheet" href="{{ style }}">
    {% endfor %}
</head>
<body>

<main>
    <!-- Page content will go here -->
    {{ content }}
</main>
</body>
</html>
